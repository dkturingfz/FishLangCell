run_name: tune_testis_v0_1
data_config: configs/data/testis.yaml
model_config: configs/model/fish_langcell_testis.yaml
training:
  seed: 42
  batch_size: 48
  max_steps: 800
  learning_rate: 0.00003
  hard_negative_ratio: 0.5
outputs:
  checkpoint_dir: outputs/checkpoints/tune_testis_v0_1
  log_dir: outputs/logs/tune_testis_v0_1
notes: "Scaffold config for testis domain specialization."
