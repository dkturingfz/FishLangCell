"""Multimodal alignment interfaces for Fish-LangCell."""


def build_multimodal_model(config_path: str) -> None:
    """Placeholder for constructing the cell-text alignment model."""
    print(f"[TODO] Build multimodal model from config: {config_path}")
